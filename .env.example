# AWS Configuration
# Configure your AWS credentials using one of these methods:
# 1. AWS CLI: Run `aws configure` in your terminal
# 2. Environment variables: Set AWS_ACCESS_KEY_ID and AWS_SECRET_ACCESS_KEY
# 3. IAM role (if running on EC2 or Lambda)

# AWS Region for Bedrock
AWS_REGION=us-east-1

# Bedrock Model ID
BEDROCK_MODEL_ID=anthropic.claude-sonnet-4-5-v2

# Agent Tools Configuration
# Set to 'true' to use AI-powered tools (Claude Haiku) for classification, entity extraction, and routing
# Set to 'false' to use mock tools (keyword matching) - default for MVP
# Note: AI-powered tools will increase API costs but provide better accuracy
USE_AGENT_TOOLS=false

# Optional: Set log level
LOG_LEVEL=INFO
